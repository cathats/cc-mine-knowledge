---
sidebar_position: 2
---

# 前端面试 QA（示例）

## Q1：React 为什么需要 key？

A：`key` 用于帮助 React 在 diff 时稳定识别列表项，减少不必要重渲染并避免状态错位。

## Q2：浏览器缓存有哪些类型？

A：常见是强缓存（`Cache-Control` / `Expires`）和协商缓存（`ETag` / `Last-Modified`）。
